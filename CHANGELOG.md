# 변경 기록

이 앱은 Phase 단위로 개발합니다. 각 Phase 가 끝나면 실행 가능한 상태를 유지하고
버전을 올립니다.

---

## v0.4.2 (versionCode 9) — 2026-09-26

**v0.4.0 · v0.4.1 코드 리뷰에서 나온 결함 수정**

### 긴급 — 이미 배포된 APK 에 있던 문제

* **엉뚱한 파일로 복원하면 기록이 전부 지워지던 문제** — 백업 형식의 모든
  항목에 기본값이 있어서, `{}` 나 다른 앱의 JSON 도 "빈 백업"으로 읽혔습니다.
  복원은 전부 대체라 기록이 통째로 사라졌습니다. 이제 백업 표시(형식 버전·
  만든 시각)가 없거나 기록이 하나도 없는 파일은 **아무것도 지우기 전에** 거절합니다.
  v0.4.0 에서 만든 백업은 그대로 복원됩니다.
* **세트 번호를 한 번 누르면 드롭세트가 되던 문제** — 워밍업이 되려면 세 번을
  눌러야 했습니다. 이제 본세트 → 워밍업 → 드롭 → 실패 순입니다.

### 기록 정확도

* **횟수 운동에 보이지 않는 중량이 들어가던 문제** — 예전에 +10kg 로 기록한 풀업이
  "횟수" 운동이 된 뒤에도 10kg 가 다음 세트로 복사되어 볼륨에 더해졌습니다.
  화면에서 숨긴 칸은 저장할 때 비웁니다(시간 운동의 중량·횟수도 같음).
* **지난 기록을 고쳐도 PR 이 그대로 남던 문제** — 끝난 운동에서 세트를 워밍업으로
  바꾸거나, 값을 고치거나, 세트·운동을 지우면 그 종목의 PR 을 처음부터 다시
  계산합니다. 잘못된 값 때문에 **뒤에 세운 PR 이 막혀 있던 경우도 되살아납니다.**
* **복원이 통째로 실패하던 경우** — 없는 루틴을 가리키는 운동 기록, 같은 id 가
  두 번 나오는 행, 이름이 같은 종목 중 하나만 있어도 복원 전체가 되돌아갔습니다.
  루틴 연결만 끊고, 중복은 하나로 합치고, 같은 이름의 종목은 합치면서 거기에
  딸린 세트를 옮겨 살립니다.
* **러닝 메모를 쓰고 뒤로 가면 사라질 수 있던 문제** — 저장이 화면과 함께
  취소될 수 있었습니다. 화면이 닫혀도 끝까지 저장합니다.

### 입력

* **시간 운동을 분·초 두 칸으로 입력** — 숫자 키패드에는 ':' 이 없어 "1:30" 을
  칠 수 없었고, 초만 받고 있었습니다.
* **직접 만든 종목도 기록 방식(중량×횟수 / 횟수 / 시간)을 고를 수 있습니다.**
  월싯처럼 시간으로 재는 운동을 추가할 수 있습니다.

### 그 밖

* 루틴 템플릿을 빠르게 두 번 누르면 루틴이 두 벌 생기던 문제.
* 시간·횟수 운동의 현재 기록 조회가 "0kg" 세 줄을 돌려주던 문제.
* 이미 있는 러닝을 저장하면 Lap·GPS 경로가 함께 지워지던 문제(앱에서 호출하는
  곳은 아직 없었습니다).

### 테스트

144개. 이번에 추가한 17개 가운데 결함을 겨냥한 12개는, 고친 부분을 모두 수정 전으로
되돌리면 정확히 그 12개만 실패하는 것을 확인했습니다.

---

## v0.4.1 (versionCode 8) — 2026-09-25

**루틴 템플릿**

헬스 탭에서 매번 루틴을 처음부터 만들지 않아도 되도록, 잘 알려진 프로그램
5개를 그대로 넣을 수 있게 했습니다. 종목 구성은 원래 알려진 그대로이며,
이미 있는 기본 종목 이름과 맞춰 자동으로 연결됩니다.

* **5x5 (초보자 전신)** — 스쿼트 중심 A/B 2일 분할
* **PPL 3분할** — 푸시 / 풀 / 레그
* **상하체 2분할**
* **3분할** — 가슴·삼두 / 등·이두 / 하체·어깨 (국내 헬스장에서 가장 흔한 구성)
* **5분할 (브로 스플릿)** — 가슴 / 등 / 어깨 / 팔 / 하체

헬스 탭 > **"유명한 루틴 템플릿에서 추가"** 에서 고르면 그 프로그램의 루틴이
한 번에 전부 만들어집니다(예: PPL 은 3개 동시 생성). 만든 뒤에는 보통
루틴처럼 자유롭게 수정할 수 있습니다.

기본 제공 종목을 지우거나 이름을 바꾼 경우, 그 종목만 건너뛰고 나머지로
루틴을 만듭니다. 하루치가 통째로 비면(예: 그날 종목을 전부 지운 경우) 그
루틴은 만들지 않습니다 — 빈 루틴은 시작 버튼이 눌리지 않아 쓸모가 없기
때문입니다.

테스트 127개 통과. 새 테스트 7개는 각각 수정 전 코드에서 실패하는 것을
확인했습니다.

---

## v0.4.0 (versionCode 7) — 2026-09-25

**다른 운동 앱과 비교해 빠져 있던 것 중, 개인 앱에 꼭 필요한 네 가지**

### 백업 / 복원 (P0-1)

서버가 없는 앱이라 기기를 바꾸거나 앱을 지우면 기록이 사라집니다.
백업 파일이 유일한 복구 수단이라 가장 먼저 필요했습니다.

* **설정 > 백업 파일 만들기** — 모든 기록과 설정을 JSON 파일 하나로 저장합니다.
  저장 위치는 안드로이드 파일 선택기로 고르므로 저장소 권한이 필요 없고,
  구글 드라이브 같은 곳에 바로 넣을 수 있습니다.
* **설정 > 백업에서 복원** — 지금 기록을 **전부 대체**합니다. 합치기는 같은
  기록이 두 벌로 늘기 쉽고 어느 쪽이 맞는지 판단할 방법이 없어 택하지
  않았습니다. 되돌릴 수 없으므로 복원 전에 한 번 더 확인받습니다.
* id 를 그대로 담기 때문에 세트-운동-기록의 연결이 복원 후에도 유지됩니다.
* 파일이 손상되어 연결이 끊어진 행이 있어도 나머지는 살립니다. 몇 개를
  건너뛰었는지 알려 줍니다.
* 읽을 수 없는 파일이나 더 새로운 형식은 기록을 건드리기 전에 거절합니다.
* **CSV 내보내기** (헬스 / 러닝) — 엑셀에서 한글이 깨지지 않도록 BOM 을 붙이고,
  쉼표가 든 값은 따옴표로 감쌉니다.

### 러닝 기록 상세 화면 (P0-2)

러닝은 저장은 되는데 완료 화면을 벗어나면 다시 열어 볼 방법이 없었습니다.

* **기록 탭에 헬스와 러닝을 시간순으로 합쳐** 보여 줍니다. 날짜가 바뀌는
  지점에만 머리글이 붙고, 전체 / 헬스 / 러닝 필터 칩이 있습니다.
* **러닝 상세** — 거리·시간·페이스·칼로리·걸음·케이던스·보폭, 목표 달성률,
  이동 경로 지도, 구간별 페이스, 메모 수정, 기록 삭제.
* **구간별 페이스를 막대로** 그려 어디서 처졌는지 바로 보이게 했습니다
  (가장 빠른 구간은 강조).
* 홈의 최근 러닝 카드를 눌러도 상세로 들어갑니다.
* 메모를 쓰다가 화면을 벗어나도 자동 저장됩니다.

### 세트 타입 — 워밍업 구분 (P0-3)

* 워밍업 / 본세트 / 드롭세트 / 실패세트. 세트 번호를 **누르면 순환**합니다
  (운동 중 조작을 늘리지 않기 위해 별도 메뉴를 두지 않았습니다).
* **워밍업은 총 볼륨·총 세트·총 반복·개인 기록에서 빠집니다.** 다음 운동의
  기본값(지난 기록)으로도 쓰지 않습니다. 가벼운 워밍업이 기록을 깎거나,
  워밍업에 잘못 친 숫자가 개인 기록으로 굳는 일이 없습니다.

### 시간·횟수 기반 운동 (P0-4)

* 종목마다 기록 방식을 둡니다: **중량 × 횟수 / 횟수 / 시간**.
* 플랭크 같은 시간 운동은 분:초로 입력하고 **최고 시간**이 개인 기록입니다.
* 풀업 같은 맨몸 운동은 **최고 횟수**가 개인 기록입니다(1RM 은 계산하지 않습니다).
* 기존 사용자의 기본 종목도 이름을 보고 채워 넣습니다(플랭크 → 시간,
  풀업·딥스·푸시업·크런치·행잉 레그레이즈·버피 → 횟수).

### DB

* 스키마 **v2 → v3**. 기존 세트는 본세트, 시간은 0 으로 채웁니다.
  v1 에서 v3 로 두 단계를 건너뛰는 경우도 포함해 마이그레이션을 검증합니다.

### 같이 고친 것

* 기본 종목 시드에 기록 방식이 빠져 플랭크가 중량 운동으로 저장되던 문제.
* `saveRun` 이 id 를 버리고 항상 새 행을 넣던 문제. 같은 기록을 저장하면
  복사본이 생겼습니다.

### 테스트

120개. 이번에 추가한 32개는 각각 수정 전 코드에서 실패하는 것을 확인했습니다.

---

## v0.3.3 (versionCode 6) — 2026-09-24

**코드 리뷰에서 나온 결함 수정**

### 심각 — 크래시

* **같은 운동을 한 세션에 두 번 넣으면 운동 완료 화면이 죽던 문제** —
  PR 을 블록마다 따로 계산해 같은 종목의 같은 종류 기록이 두 개 생겼고,
  결과 화면의 목록 키가 겹쳐 앱이 종료됐습니다. 종목 단위로 합쳐 계산합니다.
  이미 저장된 중복 기록이 있어도 화면이 깨지지 않도록 읽을 때 한 번 더 거릅니다.

### 데이터 · 정확도

* **설정이 유실되던 문제** — 읽기를 저장 블록 밖에서 하고 있어, 두 설정을
  빠르게 연달아 바꾸면 나중 것이 먼저 것을 덮어썼습니다.
* **GPS 정확도를 모를 때 0m(최상)로 취급하던 문제** — 품질을 알 수 없는 좌표가
  노이즈 필터를 그냥 통과해 거리가 부풀 수 있었고, 화면엔 "GPS ±0m"로 떴습니다.
  이제 알 수 없으면 버립니다.

### 기록 정확도

* **완료 후 횟수를 0 으로 지운 세트가 기준선을 오염시키던 문제** — 그 중량이
  영원한 최고 기록으로 남아 이후 PR 이 막혔습니다.
* **최고 볼륨 기준** — 한 세션에 같은 종목이 두 번 들어가면 합산하도록
  세션 단위로 묶습니다.
* **루틴 저장 더블탭** — 같은 루틴이 두 개 만들어지지 않습니다.
* **러닝 상태 초기화 경쟁** — 결과 화면을 닫는 처리가 직후 시작한 러닝의
  상태를 지울 수 있었습니다.

### 표시

* **자정을 넘기면 홈의 날짜·오늘의 루틴·주간 범위가 어제로 남던 문제** —
  날짜가 바뀌면 다시 계산합니다(기록 탭도 동일).
* **메모를 불러오는 동안 입력한 글자가 지워지던 문제**.

### 기타

* GPS 신호가 오래 끊겼다 큰 점프가 들어와도 Lap 이 하나로 합쳐지지 않습니다.
* 이미 완료된 세트의 중량/횟수를 고쳐도 최초 완료 시각을 유지합니다.

### 테스트

* `CodeReviewFixesTest` 7개 + 누적기 테스트 2개 추가 (총 88개).
  주요 수정은 고치기 전 코드로 되돌려 실제로 실패하는 것을 확인했습니다.

---

## v0.3.2 (versionCode 5) — 2026-09-24

**색 구성표 누락 수정**

### 고친 것

* **카드가 보라색으로 칠해지던 문제** — Material 3 의 색 역할은 서로 맞물려 있어서
  일부만 지정하면 나머지는 라이브러리 기본값(보라 계열)이 그대로 남습니다.
  테마에 표면 계열 역할을 지정하지 않아 화면의 모든 `Card` 가
  `#E6E0E9`(연보라)로 칠해지고 있었습니다. 배경은 초록빛 흰색(`#FBFDF8`)이라
  두 색이 미묘하게 어긋나 보였습니다.
* **구분선도 같은 문제** — `outlineVariant` 가 `#CAC4D0`(보라 회색)이었습니다.
* 라이트/다크 각각 11개 역할(`surfaceContainer` 5단계 · `surfaceDim` · `surfaceBright` ·
  `outlineVariant` · `inverseSurface` · `inverseOnSurface` · `inversePrimary`)을
  이 팔레트의 초록 계열 중립색으로 채웠습니다. 톤 값은 Material 3 명세를 따랐습니다.

### 추가

* `ColorSchemeTest` — 같은 문제가 다시 생기지 않도록 막습니다.
  중립 계열 역할이 팔레트를 벗어나면(파랑이 초록보다 커지면) 실패하고,
  카드·배경·버튼 위 글자가 WCAG AA(4.5:1)를 넘는지도 함께 확인합니다.

---

## v0.3.1 (versionCode 4) — 2026-09-24

**지도 화면 수정**

### 고친 것

* **경로 위에 빈 말풍선이 뜨던 문제** — osmdroid 는 경로 선을 탭하면 기본 InfoWindow 를
  띄웁니다. 보여 줄 내용이 없어 빈 회색 말풍선만 나왔습니다. 탭을 무시하도록 바꿨습니다.
  시작/끝 점도 Marker 대신 직접 그리는 방식으로 바꿔 말풍선이 생길 여지를 없앴습니다.
* **타일이 저장되지 않던 문제** — 캐시 경로를 직접 지정하면 osmdroid 가 폴더를
  만들어 주지 않습니다. 없는 폴더를 가리키고 있어서 매번 타일을 다시 받고 있었습니다.
  이제 폴더를 직접 만들어 캐시가 실제로 동작합니다.
* **짧은 러닝에서 지나치게 확대되던 문제** — 수십 미터짜리 기록도 최대 배율까지
  당겨져 주변이 보이지 않았습니다. 최소 표시 범위(약 110m)와 최대 배율(18)을 두었습니다.

### 바뀐 것

* 타일을 아직 못 받았을 때 배경이 **앱 배경색**으로 바뀌었습니다.
  osmdroid 기본값은 분홍빛 격자라 화면이 고장난 것처럼 보였습니다.
* 지도 배경을 못 받으면 **이유를 한 줄로 알려 줍니다**
  (오프라인인지, 타일 서버 오류인지).
* User-Agent 에 앱 이름과 버전을 넣었습니다. OSM 타일 서버는 앱을 식별할 수 있는
  User-Agent 를 요구하는데, 패키지명만 보내면 거부당할 수 있습니다.

---

## v0.3.0 (versionCode 3) — 2026-09-20

**러닝 화면에 걸음 수와 실제 지도 추가**

### 추가

* **걸음 수 / 케이던스** — 기기 걸음 센서(TYPE_STEP_COUNTER)로 러닝 중 걸음 수를 세고,
  분당 걸음 수(케이던스)를 함께 보여 줍니다. 결과 화면에는 평균 보폭도 나옵니다.
  일시정지 중에 걸은 걸음은 더해지지 않습니다.
* **실제 지도** — OpenStreetMap 위에 달린 경로를 그립니다. 러닝 진행 화면에서
  우측 상단 버튼으로 **큰 숫자 화면 ↔ 지도 화면**을 한 번에 전환할 수 있고,
  결과 화면에서는 전체 경로를 지도에서 확인할 수 있습니다.
* **러닝 시작 화면** — 걸음 센서 사용 여부를 미리 알려 줍니다.

### 바뀐 것

* 러닝 진행 화면이 두 가지 모드가 되었습니다. 기본은 이전과 같은 큰 숫자 화면이라
  "달리면서 조작을 최소화한다" 는 원칙은 그대로입니다.
* 결과 화면의 경로 그림이 Canvas 직접 그리기에서 실제 지도로 바뀌었습니다.

### 권한

| 권한 | 용도 | 없으면 |
| --- | --- | --- |
| `ACTIVITY_RECOGNITION` | 걸음 수 세기 | 걸음 수만 숨김, 러닝 기록은 정상 |
| `INTERNET` · `ACCESS_NETWORK_STATE` | 지도 타일 받기 | 경로 선은 그대로 보이고 배경만 비어 있음 |

> 기록은 여전히 전부 기기 안에만 저장됩니다. 네트워크는 지도 타일을 받는 데에만 씁니다.
> 받은 타일은 캐시되어 같은 지역은 다음부터 오프라인에서도 보입니다.

### 데이터베이스

* 스키마 **v1 → v2**: `run` 테이블에 `steps` 컬럼 추가.
* 기존 기록은 마이그레이션으로 그대로 유지되고, 예전 러닝의 걸음 수는 0 이 됩니다.

---

## v0.2.0 (versionCode 2) — 2026-09-20

**Phase 2: GPS 러닝 기록**

### 추가

* **러닝 기록** — Foreground Service(`foregroundServiceType=location`)로 GPS 를 수신해
  화면이 꺼져 있어도 거리와 시간이 계속 쌓입니다.
* **러닝 시작 화면** — 위치 권한과 GPS 상태를 확인하고, 자유 러닝 / 목표 거리 / 목표 시간
  중에서 고릅니다. 목표에 도달하면 자동으로 종료됩니다.
* **러닝 진행 화면** — 거리와 현재 페이스를 가장 크게 표시하고, 하단에 큰 일시정지 버튼
  하나만 둡니다. 달리는 동안 화면이 꺼지지 않습니다.
* **자동 Lap** — 기본 1km(설정에서 변경 가능)마다 Lap 을 만들고, 종료 시 1km 를 채우지 못한
  자투리도 마지막 Lap 으로 남깁니다.
* **러닝 결과 화면** — 거리 · 시간 · 평균/최고 페이스 · 칼로리 · Lap 목록 · 이동 경로 ·
  개인 기록(최장 거리 / 최고 평균 페이스) · 메모.
* **이동 경로 그리기** — 지도 SDK 없이 Compose Canvas 로 그립니다.
  API 키와 네트워크가 필요 없어 오프라인 원칙에 맞습니다.
* **홈 화면 "러닝 기록 중" 카드** — 기록 중인 러닝으로 바로 돌아갈 수 있습니다.
* **설정: 체중** — 러닝 칼로리 추정에 사용합니다(기본 70kg).

### 기록 정확도

* 정확도 30m 초과, 3m 미만 이동, 12m/s 초과 속도의 GPS 샘플은 버립니다.
* 현재 페이스는 최근 30초 구간으로 계산하고, 최고 페이스는 100m 이상 구간에서만
  후보로 잡습니다.
* 일시정지 구간의 이동은 거리에 더하지 않고 경로선도 끊습니다.

### 저장

* 러닝을 시작할 때 `run` 행을 먼저 만들고 5초마다 경로와 누적값을 증분 저장합니다.
  기록 도중 앱이 죽어도 이미 달린 구간이 남습니다.

### 빌드

* 릴리즈 서명 설정을 추가했습니다(`keystore.properties` 또는 환경 변수).
* 자세한 내용은 [docs/RELEASE.md](docs/RELEASE.md) 참고.

### 알려진 제한

* 저장된 러닝은 **홈의 "최근 러닝 기록"** 에서만 볼 수 있습니다.
  기록 탭의 캘린더와 러닝 상세 화면은 Phase 3 에서 붙습니다.

---

## v0.1.0 (versionCode 1) — 2026-09-20

**Phase 1: 헬스 운동 기록 + 프로젝트 기반**

### 추가

* **프로젝트 기반** — Kotlin 2.0 · Compose · Material 3 · Hilt · Room(KSP) · DataStore,
  UI / ViewModel / Repository / Database 계층 분리, 오프라인 전용(서버 없음).
* **홈** — 이번 주 요약(헬스 횟수 · 러닝 거리 · 총 운동시간), 오늘 예정된 루틴,
  운동 시작 버튼, 최근 기록, 진행 중인 운동 이어하기.
* **루틴** — 생성 · 수정 · 삭제, 운동 추가와 순서 변경, 기본 세트 수와 휴식시간 설정,
  요일 지정(지정한 요일에 홈에 표시).
* **운동 진행** — 지난 기록을 기본값으로 자동 입력, 큰 세트 완료 버튼, 세트 추가/삭제,
  운동 중 종목 추가.
* **휴식 타이머** — 세트 완료 시 자동 시작, ±10초 · 일시정지 · 건너뛰기, 종료 시 진동과 알림.
* **운동 종료** — 총 운동시간 · 완료한 운동 수 · 총 세트 · 총 반복 · 총 볼륨 요약,
  새로 세운 PR 강조, 메모.
* **PR** — 최고 중량 / 최고 볼륨 / 예상 1RM(Epley). 종료 시점에 판정해 DB 에 저장하므로
  나중에 기록을 다시 열어도 그대로 보입니다.
* **기록** — 목록과 상세, 세트 값 수정, 메모 수정, 삭제.
* **설정** — 기본 휴식시간 · 중량/거리 단위 · 진동 · 자동 Lap 거리 · 테마(Light/Dark/시스템).
* **기본 운동 종목 41개** — 첫 실행 시 DB 생성 트랜잭션 안에서 넣습니다.

### 설계

* 운동 세션은 **시작하는 순간 DB 에 먼저 기록**합니다(`endTime = null` 이 진행 중).
  앱이 죽어도 홈의 "이어하기" 로 복구됩니다.
* 러닝 테이블(`run` / `run_lap` / `run_location`)을 스키마 v1 에 미리 넣어
  Phase 2 에서 마이그레이션이 필요 없게 했습니다.
* 중량은 kg, 거리는 meter 로만 저장하고 단위 변환은 화면에서만 합니다.
